-- Baseline migration (expand-contract strategy, ADR/database conventions).
-- Intentionally empty: per-feature tables are added in subsequent versioned
-- migrations (V2 = beneficiário, V3 = programa, V4 = pagamento, V5 = auditoria).
SELECT 1;
